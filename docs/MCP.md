## GIAI ĐOẠN 3: THIẾT KẾ UNIFIED MCP - "CLAUDE CODE GUARDIAN"

---

### 1. NAMING & BRANDING

| Aspect | Value |
|--------|-------|
| **Tên MCP** | **Claude Code Guardian** (CCG) |
| **Tagline** | "Your AI Coding Assistant's Safety Net" |
| **Package** | `@anthropic-community/claude-code-guardian` |
| **Command prefix** | `/ccg` hoặc `/guardian` |

---

### 2. KIẾN TRÚC UNIFIED MCP

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                      CLAUDE CODE GUARDIAN (CCG)                             │
│                         Unified MCP Server                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         CORE ENGINE                                  │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │   │
│  │  │   Event     │  │   State     │  │   Config    │  │   Logger    │ │   │
│  │  │   Bus       │  │   Manager   │  │   Manager   │  │   Service   │ │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘ │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         TOOL MODULES                                 │   │
│  │                                                                      │   │
│  │  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐   │   │
│  │  │  🧠 Memory       │  │  🔍 Intelligence │  │  ⚡ Workflow      │   │   │
│  │  │  - store        │  │  - search        │  │  - task_start    │   │   │
│  │  │  - recall       │  │  - analyze       │  │  - task_complete │   │   │
│  │  │  - forget       │  │  - index         │  │  - checkpoint    │   │   │
│  │  └──────────────────┘  └──────────────────┘  └──────────────────┘   │   │
│  │                                                                      │   │
│  │  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐   │   │
│  │  │  🛡️ Guard        │  │  🧪 Testing      │  │  📁 Documents    │   │   │
│  │  │  - validate     │  │  - run_tests    │  │  - register      │   │   │
│  │  │  - check_honest │  │  - cleanup      │  │  - update        │   │   │
│  │  │  - block        │  │  - isolate      │  │  - find          │   │   │
│  │  └──────────────────┘  └──────────────────┘  └──────────────────┘   │   │
│  │                                                                      │   │
│  │  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐   │   │
│  │  │  🖥️ Process      │  │  📊 Resource     │  │  🌐 Browser      │   │   │
│  │  │  - port_check   │  │  - token_status │  │  - open          │   │   │
│  │  │  - kill         │  │  - estimate     │  │  - screenshot    │   │   │
│  │  │  - spawn        │  │  - checkpoint   │  │  - capture_logs  │   │   │
│  │  └──────────────────┘  └──────────────────┘  └──────────────────┘   │   │
│  │                                                                      │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         STORAGE LAYER                                │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │   │
│  │  │  SQLite     │  │  Vector     │  │  JSON       │  │  File       │ │   │
│  │  │  (Memory)   │  │  (Embeddings│  │  (Config)   │  │  (Registry) │ │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘ │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 3. UX FLOW NGƯỜI DÙNG

#### 3.1. Session Lifecycle Flow

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        USER SESSION LIFECYCLE                               │
└─────────────────────────────────────────────────────────────────────────────┘

     USER                           CLAUDE CODE                    CCG MCP
       │                                 │                            │
       │  $ claude                       │                            │
       │────────────────────────────────>│                            │
       │                                 │   [SessionStart Hook]      │
       │                                 │───────────────────────────>│
       │                                 │                            │
       │                                 │   ┌────────────────────┐   │
       │                                 │   │ 1. Load Memory     │   │
       │                                 │   │ 2. Check Processes │   │
       │                                 │   │ 3. Load Tasks      │   │
       │                                 │   │ 4. Index Check     │   │
       │                                 │   └────────────────────┘   │
       │                                 │                            │
       │                                 │<───────────────────────────│
       │                                 │   Context Injected         │
       │                                 │                            │
       │  ┌──────────────────────────────────────────────────────┐    │
       │  │ 🟢 CCG Ready | Memory: 23 items | Tasks: 2 pending   │    │
       │  │ 💡 Resume: "Implement user auth" - 70% complete      │    │
       │  └──────────────────────────────────────────────────────┘    │
       │                                 │                            │
       │  "Add login form"               │                            │
       │────────────────────────────────>│                            │
       │                                 │   [PreTask Validation]     │
       │                                 │───────────────────────────>│
       │                                 │                            │
       │                                 │   ┌────────────────────┐   │
       │                                 │   │ • Estimate: ~2000  │   │
       │                                 │   │   tokens           │   │
       │                                 │   │ • Impact: 3 files  │   │
       │                                 │   │ • Tests: 5 affected│   │
       │                                 │   └────────────────────┘   │
       │                                 │                            │
       │  ┌──────────────────────────────────────────────────────┐    │
       │  │ 📋 Task Analysis:                                     │    │
       │  │ • Estimated: ~2000 tokens (within budget ✓)          │    │
       │  │ • Files affected: Login.tsx, auth.ts, routes.ts      │    │
       │  │ • Tests to run: auth.test.ts, Login.test.tsx         │    │
       │  └──────────────────────────────────────────────────────┘    │
       │                                 │                            │
       │                                 │   [During Execution]       │
       │                                 │<─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─>│
       │                                 │   Real-time validation     │
       │                                 │                            │
       │  ┌──────────────────────────────────────────────────────┐    │
       │  │ ⚠️  CCG Warning: Empty catch block detected          │    │
       │  │    Line 45: catch(e) { }                             │    │
       │  │    Suggestion: Add error handling or logging         │    │
       │  └──────────────────────────────────────────────────────┘    │
       │                                 │                            │
       │                                 │   [PostTask Validation]    │
       │                                 │───────────────────────────>│
       │                                 │                            │
       │                                 │   ┌────────────────────┐   │
       │                                 │   │ • Run tests        │   │
       │                                 │   │ • Browser check    │   │
       │                                 │   │ • Save progress    │   │
       │                                 │   └────────────────────┘   │
       │                                 │                            │
       │  ┌──────────────────────────────────────────────────────┐    │
       │  │ ✅ Task Complete:                                     │    │
       │  │ • Tests: 5/5 passed                                  │    │
       │  │ • Browser: No console errors                         │    │
       │  │ • Checkpoint saved                                   │    │
       │  └──────────────────────────────────────────────────────┘    │
       │                                 │                            │
```

#### 3.2. Visual Status Bar (Always Visible)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         CCG STATUS BAR                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  Normal State:                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ 🛡️ CCG │ 🧠 24 │ 📊 45% │ ⚡ Task: auth-login │ 🟢 All Clear       │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│       │      │       │              │                    │                  │
│       │      │       │              │                    └─ Status          │
│       │      │       │              └─ Current Task                         │
│       │      │       └─ Token Usage                                         │
│       │      └─ Memory Items                                                │
│       └─ Guardian Active                                                    │
│                                                                             │
│  Warning State:                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ 🛡️ CCG │ 🧠 24 │ 📊 85% ⚠️ │ ⚡ Task: auth-login │ 🟡 1 Warning    │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  Critical State:                                                            │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ 🛡️ CCG │ 🧠 24 │ 📊 95% 🔴 │ ⚡ CHECKPOINT NOW │ 🔴 Action Required │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 4. SLASH COMMANDS & INTERACTIONS

#### 4.1. Command Structure

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         CCG SLASH COMMANDS                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  /ccg                     → Show dashboard & quick actions                  │
│  /ccg status              → Detailed status of all modules                  │
│  /ccg help                → Show all available commands                     │
│                                                                             │
│  ─────────────────────── MEMORY ───────────────────────                     │
│  /ccg memory              → Show memory summary                             │
│  /ccg memory search <q>   → Search memories                                 │
│  /ccg memory save <note>  → Save important note                             │
│  /ccg memory clear        → Clear session memory (confirm required)         │
│                                                                             │
│  ─────────────────────── TASKS ────────────────────────                     │
│  /ccg task                → Show current task status                        │
│  /ccg task start <name>   → Start new task with tracking                    │
│  /ccg task done           → Mark current task complete                      │
│  /ccg task list           → List all pending tasks                          │
│  /ccg task resume         → Resume last incomplete task                     │
│                                                                             │
│  ─────────────────────── CHECKPOINT ───────────────────                     │
│  /ccg checkpoint          → Create manual checkpoint                        │
│  /ccg checkpoint list     → List all checkpoints                            │
│  /ccg checkpoint restore  → Restore from checkpoint                         │
│                                                                             │
│  ─────────────────────── TESTING ──────────────────────                     │
│  /ccg test                → Run affected tests                              │
│  /ccg test all            → Run all tests                                   │
│  /ccg test browser        → Open browser for UI testing                     │
│  /ccg test cleanup        → Clean test data                                 │
│                                                                             │
│  ─────────────────────── PROCESS ──────────────────────                     │
│  /ccg process             → Show running processes                          │
│  /ccg process kill <port> → Kill process on port                            │
│  /ccg process cleanup     → Kill all spawned processes                      │
│                                                                             │
│  ─────────────────────── DOCS ─────────────────────────                     │
│  /ccg docs                → Show document registry                          │
│  /ccg docs find <name>    → Find document by name                           │
│  /ccg docs update <path>  → Mark doc for update (not create new)            │
│                                                                             │
│  ─────────────────────── GUARD ────────────────────────                     │
│  /ccg guard               → Show guard status & recent blocks               │
│  /ccg guard disable       → Temporarily disable (requires confirm)          │
│  /ccg guard report        → Generate honesty report                         │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 4.2. Interactive Dashboard (`/ccg`)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     🛡️ CLAUDE CODE GUARDIAN                                 │
│                        Interactive Dashboard                                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         QUICK STATUS                                 │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │  🧠 Memory      │ 24 items │ Last saved: 5 min ago                  │   │
│  │  📊 Resources   │ 45% used │ ~5500 tokens remaining                 │   │
│  │  ⚡ Task        │ auth-login │ In Progress (70%)                    │   │
│  │  🛡️ Guard       │ Active   │ 0 blocks today                        │   │
│  │  🖥️ Processes   │ 2 running │ Ports: 3000, 5432                     │   │
│  │  🧪 Tests       │ Ready    │ 12 tests, 0 failing                    │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                       QUICK ACTIONS                                  │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │  [1] 💾 Save Checkpoint    [2] 🧪 Run Tests    [3] 🌐 Open Browser  │   │
│  │  [4] 📋 View Tasks         [5] 🧠 Search Memory [6] 📊 Token Report │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                       RECENT ACTIVITY                                │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │  10:45 │ ✅ Checkpoint saved: auth-login-70%                        │   │
│  │  10:42 │ 🧪 Tests passed: 5/5                                       │   │
│  │  10:38 │ ⚠️ Warning: Large file edit (500+ lines)                   │   │
│  │  10:35 │ 📁 Document updated: AUTH.md                               │   │
│  │  10:30 │ ⚡ Task started: auth-login                                 │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  Type number for quick action, or /ccg <command> for specific action       │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 5. HOOKS INTEGRATION

#### 5.1. Hook Configuration (`.claude/hooks.json`)

```json
{
  "hooks": {
    "SessionStart": [
      {
        "type": "mcp",
        "server": "claude-code-guardian",
        "tool": "session_init",
        "description": "Initialize CCG on session start"
      }
    ],
    "PreToolCall": [
      {
        "type": "mcp",
        "server": "claude-code-guardian", 
        "tool": "pre_validate",
        "description": "Validate before file operations",
        "filter": {
          "tools": ["write_file", "edit_file", "bash"]
        }
      }
    ],
    "PostToolCall": [
      {
        "type": "mcp",
        "server": "claude-code-guardian",
        "tool": "post_validate", 
        "description": "Validate after file operations",
        "filter": {
          "tools": ["write_file", "edit_file"]
        }
      }
    ],
    "Stop": [
      {
        "type": "mcp",
        "server": "claude-code-guardian",
        "tool": "session_end",
        "description": "Cleanup and save on session end"
      }
    ]
  }
}
```

#### 5.2. Hook Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           HOOKS FLOW                                        │
└─────────────────────────────────────────────────────────────────────────────┘

  SESSION START                    DURING SESSION                  SESSION END
       │                                 │                              │
       ▼                                 │                              │
┌──────────────┐                         │                              │
│ SessionStart │                         │                              │
│    Hook      │                         │                              │
├──────────────┤                         │                              │
│ • Load memory│                         │                              │
│ • Check proc │                         │                              │
│ • Load tasks │                         │                              │
│ • Show status│                         │                              │
└──────────────┘                         │                              │
       │                                 │                              │
       ▼                                 ▼                              │
       ─────────────────────────────────────────────────────────────    │
                                         │                              │
                           ┌─────────────┴─────────────┐                │
                           │      User Request         │                │
                           │   "Add login feature"     │                │
                           └─────────────┬─────────────┘                │
                                         │                              │
                                         ▼                              │
                           ┌──────────────────────────┐                 │
                           │     PreToolCall Hook     │                 │
                           ├──────────────────────────┤                 │
                           │ • Estimate tokens        │                 │
                           │ • Check budget           │                 │
                           │ • Analyze impact         │                 │
                           │ • Validate action        │                 │
                           └────────────┬─────────────┘                 │
                                        │                               │
                          ┌─────────────┴─────────────┐                 │
                          │                           │                 │
                          ▼                           ▼                 │
                    ┌──────────┐               ┌──────────┐             │
                    │ ALLOWED  │               │ BLOCKED  │             │
                    └────┬─────┘               └────┬─────┘             │
                         │                          │                   │
                         ▼                          ▼                   │
                   ┌──────────┐            ┌────────────────┐           │
                   │ Execute  │            │ Show Warning   │           │
                   │ Tool     │            │ + Suggestion   │           │
                   └────┬─────┘            └────────────────┘           │
                        │                                               │
                        ▼                                               │
              ┌──────────────────────────┐                              │
              │    PostToolCall Hook     │                              │
              ├──────────────────────────┤                              │
              │ • Validate output        │                              │
              │ • Check for issues       │                              │
              │ • Run quick tests        │                              │
              │ • Update memory          │                              │
              │ • Auto-checkpoint if 50% │                              │
              └──────────────────────────┘                              │
                        │                                               │
                        ▼                                               │
              ┌──────────────────────────┐                              │
              │   Token Check (85%?)     │──── YES ────┐                │
              └──────────────────────────┘             │                │
                        │ NO                           ▼                │
                        │                   ┌──────────────────┐        │
                        │                   │ Force Checkpoint │        │
                        │                   │ + Warning        │        │
                        │                   └──────────────────┘        │
                        │                              │                │
                        └──────────────────────────────┘                │
                                         │                              │
       ──────────────────────────────────┴──────────────────────────    │
                                                                        │
                                                                        ▼
                                                              ┌──────────────┐
                                                              │     Stop     │
                                                              │     Hook     │
                                                              ├──────────────┤
                                                              │ • Save memory│
                                                              │ • Save tasks │
                                                              │ • Cleanup    │
                                                              │ • Kill procs │
                                                              │ • Final ckpt │
                                                              └──────────────┘
```

---

### 6. VISUAL NOTIFICATIONS & ALERTS

#### 6.1. Notification Types

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         NOTIFICATION TYPES                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ────────────────────── INFO (Blue) ──────────────────────                  │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ ℹ️  CCG: Checkpoint saved automatically (50% token usage)           │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ────────────────────── SUCCESS (Green) ──────────────────                  │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ ✅ CCG: All 5 tests passed! No regressions detected.                │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ────────────────────── WARNING (Yellow) ─────────────────                  │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ ⚠️  CCG Warning: Token usage at 85%                                  │   │
│  │    Recommendation: Complete current task and checkpoint              │   │
│  │    [Save Checkpoint] [Continue Anyway]                               │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ────────────────────── ERROR (Red) ──────────────────────                  │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ 🔴 CCG BLOCK: Potential fake test detected!                          │   │
│  │                                                                      │   │
│  │    File: auth.test.ts                                                │   │
│  │    Issue: Test has no assertions (expect/assert)                     │   │
│  │                                                                      │   │
│  │    test('should login user', () => {                                 │   │
│  │      const result = login('user', 'pass');                           │   │
│  │      // Missing assertion!                                           │   │
│  │    });                                                               │   │
│  │                                                                      │   │
│  │    Action Required: Add proper assertions                            │   │
│  │    [Fix Now] [Show Example] [Override (Not Recommended)]             │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ────────────────────── CRITICAL (Red + Sound) ───────────                  │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ 🚨 CCG CRITICAL: Feature disabled to avoid error!                    │   │
│  │                                                                      │   │
│  │    Detected: Commenting out authentication middleware                │   │
│  │    File: middleware/auth.ts                                          │   │
│  │    Lines: 45-52                                                      │   │
│  │                                                                      │   │
│  │    This action has been BLOCKED.                                     │   │
│  │    Disabling security features is not allowed.                       │   │
│  │                                                                      │   │
│  │    [Show Original Error] [Get Help Fixing]                           │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 6.2. Inline Code Annotations

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                      INLINE CODE ANNOTATIONS                                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  When Claude generates code, CCG adds inline annotations:                   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  1  │ // Login.tsx                                                  │   │
│  │  2  │ import { useState } from 'react';                             │   │
│  │  3  │                                                               │   │
│  │  4  │ export function Login() {                                     │   │
│  │  5  │   const [email, setEmail] = useState('');                     │   │
│  │  6  │   const [password, setPassword] = useState('');               │   │
│  │  7  │                                                               │   │
│  │  8  │   const handleSubmit = async () => {                          │   │
│  │  9  │     try {                                                     │   │
│  │ 10  │       const result = await login(email, password);            │   │
│  │ 11  │     } catch (e) {                                             │   │
│  │ 12  │       // TODO: handle error  ← ⚠️ CCG: Empty catch block      │   │
│  │ 13  │     }                                                         │   │
│  │ 14  │   };                                                          │   │
│  │ 15  │                                                               │   │
│  │ 16  │   return (                                                    │   │
│  │ 17  │     <form onSubmit={handleSubmit}>                            │   │
│  │ 18  │       {/* Form content */}                                    │   │
│  │ 19  │     </form>                                                   │   │
│  │ 20  │   );                                                          │   │
│  │ 21  │ }                                                             │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ 📋 CCG Code Review:                                                  │   │
│  │                                                                      │   │
│  │  ⚠️ Line 12: Empty catch block                                       │   │
│  │     → Add error handling: setError(e.message)                        │   │
│  │                                                                      │   │
│  │  ✅ Line 5-6: Variables follow naming convention                     │   │
│  │  ✅ Line 8: Async function properly structured                       │   │
│  │                                                                      │   │
│  │  [Auto-Fix All] [Ignore Warnings]                                    │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 7. CONFIGURATION FILE (`.ccg/config.json`)

```json
{
  "version": "1.0.0",
  "project": {
    "name": "my-project",
    "type": "typescript-react"
  },
  
  "modules": {
    "memory": {
      "enabled": true,
      "maxItems": 100,
      "autoSave": true,
      "persistPath": ".ccg/memory.db"
    },
    
    "guard": {
      "enabled": true,
      "strictMode": true,
      "rules": {
        "blockFakeTests": true,
        "blockDisabledFeatures": true,
        "blockEmptyCatch": true,
        "blockEmojiInCode": true
      }
    },
    
    "process": {
      "enabled": true,
      "ports": {
        "frontend": 3000,
        "backend": 8000,
        "database": 5432
      },
      "autoKillOnConflict": true
    },
    
    "resource": {
      "enabled": true,
      "checkpoints": {
        "auto": true,
        "thresholds": [50, 70, 85, 95]
      },
      "warningAt": 85,
      "pauseAt": 95
    },
    
    "testing": {
      "enabled": true,
      "autoRun": true,
      "browser": {
        "enabled": true,
        "headless": false,
        "captureConsole": true,
        "captureNetwork": true
      },
      "cleanup": {
        "autoCleanTestData": true,
        "testDataPrefix": "test_"
      }
    },
    
    "documents": {
      "enabled": true,
      "locations": {
        "docs": "./docs",
        "specs": "./docs/specs",
        "api": "./docs/api"
      },
      "updateInsteadOfCreate": true
    }
  },
  
  "notifications": {
    "showInline": true,
    "showStatusBar": true,
    "sound": {
      "enabled": false,
      "criticalOnly": true
    }
  },
  
  "conventions": {
    "fileNaming": "kebab-case",
    "variableNaming": "camelCase",
    "componentNaming": "PascalCase",
    "noEmoji": true,
    "noUnicode": true
  }
}
```

---

### 8. PROJECT STRUCTURE

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        PROJECT STRUCTURE                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  your-project/                                                              │
│  ├── .ccg/                          # CCG data directory                    │
│  │   ├── config.json                # Configuration                         │
│  │   ├── memory.db                  # SQLite memory storage                 │
│  │   ├── checkpoints/               # Checkpoint files                      │
│  │   │   ├── 2025-11-29-auth-70.json                                       │
│  │   │   └── 2025-11-29-auth-100.json                                      │
│  │   ├── tasks/                     # Task tracking                         │
│  │   │   └── current.json                                                  │
│  │   ├── registry/                  # Document & test registry              │
│  │   │   ├── documents.json                                                │
│  │   │   └── tests.json                                                    │
│  │   └── logs/                      # Activity logs                         │
│  │       └── 2025-11-29.log                                                │
│  │                                                                          │
│  ├── .claude/                       # Claude Code config                    │
│  │   ├── hooks.json                 # CCG hooks                             │
│  │   ├── commands/                  # Slash commands                        │
│  │   │   └── ccg.md                 # CCG command definitions               │
│  │   └── settings.json              # Claude settings                       │
│  │                                                                          │
│  ├── .mcp.json                      # MCP server config                     │
│  │                                                                          │
│  └── ... (your project files)                                              │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 9. MCP TOOLS SPECIFICATION

```typescript
// CCG MCP Tools Definition

interface CCGTools {
  // ═══════════════════ SESSION ═══════════════════
  session_init: {
    description: "Initialize CCG session, load memory, check processes";
    params: {};
    returns: SessionStatus;
  };
  
  session_end: {
    description: "End session, save all data, cleanup";
    params: {};
    returns: SessionSummary;
  };

  // ═══════════════════ MEMORY ═══════════════════
  memory_store: {
    description: "Store information in persistent memory";
    params: {
      content: string;
      type: "decision" | "fact" | "code_pattern" | "error" | "note";
      importance: number; // 1-10
      tags?: string[];
    };
    returns: { id: string; stored: boolean };
  };
  
  memory_recall: {
    description: "Search and retrieve memories";
    params: {
      query: string;
      limit?: number;
      type?: string;
    };
    returns: Memory[];
  };
  
  memory_forget: {
    description: "Remove specific memory";
    params: { id: string };
    returns: { deleted: boolean };
  };

  // ═══════════════════ TASK ═══════════════════
  task_start: {
    description: "Start tracking a new task";
    params: {
      name: string;
      description?: string;
      estimatedTokens?: number;
    };
    returns: Task;
  };
  
  task_update: {
    description: "Update task progress";
    params: {
      progress: number; // 0-100
      note?: string;
    };
    returns: Task;
  };
  
  task_complete: {
    description: "Mark task as complete";
    params: {
      summary?: string;
    };
    returns: TaskCompletion;
  };

  // ═══════════════════ CHECKPOINT ═══════════════════
  checkpoint_create: {
    description: "Create a checkpoint";
    params: {
      name?: string;
      reason?: string;
    };
    returns: Checkpoint;
  };
  
  checkpoint_restore: {
    description: "Restore from checkpoint";
    params: { id: string };
    returns: RestoreResult;
  };

  // ═══════════════════ GUARD ═══════════════════
  guard_validate: {
    description: "Validate code before writing";
    params: {
      code: string;
      filename: string;
      action: "create" | "edit";
    };
    returns: ValidationResult;
  };
  
  guard_check_test: {
    description: "Check if test is legitimate";
    params: {
      testCode: string;
      filename: string;
    };
    returns: TestValidation;
  };

  // ═══════════════════ PROCESS ═══════════════════
  process_check_port: {
    description: "Check if port is available";
    params: { port: number };
    returns: PortStatus;
  };
  
  process_kill: {
    description: "Kill process on port";
    params: { port: number };
    returns: { killed: boolean; pid?: number };
  };
  
  process_spawn: {
    description: "Spawn and track a process";
    params: {
      command: string;
      port?: number;
      name?: string;
    };
    returns: Process;
  };

  // ═══════════════════ TESTING ═══════════════════
  test_run: {
    description: "Run tests";
    params: {
      files?: string[];
      all?: boolean;
    };
    returns: TestResults;
  };
  
  test_browser_open: {
    description: "Open browser for testing";
    params: {
      url: string;
      captureConsole?: boolean;
      captureNetwork?: boolean;
    };
    returns: BrowserSession;
  };
  
  test_browser_screenshot: {
    description: "Take browser screenshot";
    params: {
      sessionId: string;
      selector?: string;
    };
    returns: Screenshot;
  };
  
  test_cleanup: {
    description: "Clean up test data";
    params: {};
    returns: CleanupResult;
  };

  // ═══════════════════ DOCUMENTS ═══════════════════
  doc_find: {
    description: "Find existing document";
    params: {
      name: string;
      type?: string;
    };
    returns: Document | null;
  };
  
  doc_register: {
    description: "Register new document location";
    params: {
      path: string;
      type: string;
      description?: string;
    };
    returns: { registered: boolean };
  };
  
  doc_should_update: {
    description: "Check if should update existing doc";
    params: {
      proposedPath: string;
      content: string;
    };
    returns: {
      existingDoc: Document | null;
      recommendation: "update" | "create";
    };
  };

  // ═══════════════════ RESOURCE ═══════════════════
  resource_status: {
    description: "Get resource usage status";
    params: {};
    returns: ResourceStatus;
  };
  
  resource_estimate: {
    description: "Estimate task resource needs";
    params: {
      taskDescription: string;
      filesAffected?: string[];
    };
    returns: ResourceEstimate;
  };

  // ═══════════════════ STATUS ═══════════════════
  status_full: {
    description: "Get full CCG status";
    params: {};
    returns: FullStatus;
  };
  
  status_quick: {
    description: "Get quick status for status bar";
    params: {};
    returns: QuickStatus;
  };
}
```

---

### 10. INSTALLATION & SETUP

```bash
# ═══════════════════════════════════════════════════════════════
#                    INSTALLATION GUIDE
# ═══════════════════════════════════════════════════════════════

# Step 1: Install CCG MCP Server
npm install -g @anthropic-community/claude-code-guardian

# Step 2: Add to Claude Code (User scope - available everywhere)
claude mcp add claude-code-guardian -s user -- npx @anthropic-community/claude-code-guardian

# Step 3: Initialize in your project
cd your-project
ccg init

# This creates:
# - .ccg/config.json (configuration)
# - .claude/hooks.json (hooks integration)
# - .claude/commands/ccg.md (slash commands)

# Step 4: Verify installation
claude
> /ccg status

# ═══════════════════════════════════════════════════════════════
#                    QUICK START
# ═══════════════════════════════════════════════════════════════

# Start Claude Code - CCG automatically initializes
claude

# You'll see:
# 🛡️ CCG Ready | 🧠 0 | 📊 0% | ⚡ No task | 🟢 All Clear
# 
# 💡 Welcome! CCG is active. Type /ccg for dashboard.

# Start a task
> /ccg task start "implement-login"

# Work normally - CCG monitors in background
> Add a login form with email and password validation

# Check status anytime
> /ccg

# Create manual checkpoint
> /ccg checkpoint

# Run tests
> /ccg test

# End session - CCG auto-saves
> /exit
```

---

### 11. SUMMARY: USER EXPERIENCE FLOW

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     COMPLETE UX FLOW SUMMARY                                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   1. USER OPENS CLAUDE CODE                                                 │
│      └─→ CCG auto-initializes via SessionStart hook                         │
│      └─→ Status bar shows: 🛡️ CCG Ready                                     │
│      └─→ Previous context restored automatically                            │
│                                                                             │
│   2. USER STARTS WORKING                                                    │
│      └─→ /ccg task start "feature-name" (optional but recommended)          │
│      └─→ CCG tracks progress, estimates resources                           │
│                                                                             │
│   3. USER GIVES CODING REQUEST                                              │
│      └─→ PreToolCall hook validates action                                  │
│      └─→ Impact analysis shown (files affected, tests needed)               │
│      └─→ Resource estimate provided                                         │
│                                                                             │
│   4. CLAUDE GENERATES CODE                                                  │
│      └─→ Guard validates in real-time                                       │
│      └─→ Blocks dishonest behaviors (fake tests, disabled features)         │
│      └─→ Warnings for issues (empty catch, inconsistent naming)             │
│                                                                             │
│   5. CODE IS WRITTEN                                                        │
│      └─→ PostToolCall hook runs tests                                       │
│      └─→ Browser testing if UI changes                                      │
│      └─→ Auto-checkpoint at thresholds (50%, 70%, 85%)                      │
│                                                                             │
│   6. TOKEN USAGE WARNINGS                                                   │
│      └─→ 85%: Warning + recommendation                                      │
│      └─→ 95%: Force checkpoint + pause                                      │
│      └─→ User can /ccg checkpoint restore in new session                    │
│                                                                             │
│   7. USER FINISHES / EXITS                                                  │
│      └─→ Stop hook saves everything                                         │
│      └─→ Memory compressed and stored                                       │
│      └─→ Tasks saved for resume                                             │
│      └─→ Processes cleaned up                                               │
│                                                                             │
│   8. NEXT SESSION                                                           │
│      └─→ "Resume task: feature-name (70% complete)"                         │
│      └─→ Context restored seamlessly                                        │
│      └─→ Continue where left off                                            │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## NEXT STEPS

Bạn muốn tiếp tục với bước nào?

1. **Chi tiết Implementation** - Code structure, TypeScript interfaces, MCP SDK setup

2. **Module cụ thể** - Deep dive vào một module (Guard, Process, Memory...)

3. **Prototype** - Tạo một working prototype với core features

4. **Testing Strategy** - Cách test MCP server này